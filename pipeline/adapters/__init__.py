"""Portal adapters. Each adapter emits the canonical schema (see
pipeline/schema.sql). mahatenders is implemented (Tier 1); Tier 2/3 portals are
registered in sources/registry.yaml and added one at a time."""
